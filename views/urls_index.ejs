<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<ul>
   <% for (url in urls) { %>
   <li><%= url %> : <%= urls[url]%></li>
   <% } %>
  </ul>
<body>
 <a href="url">EDIT</a>
</body>
</html>